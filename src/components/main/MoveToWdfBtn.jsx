import styled from "styled-components";
export default function MoveToWdfBtn() {
  return (
    <>
      <Button>와디페로 이동</Button>
    </>
  );
}

const Button = styled.button`
  position: fixed;
  bottom: 0.5rem;
`;
